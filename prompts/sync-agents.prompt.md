---
description: Re-run the .agents setup script to sync skills, context, prompts, and MCP config
---

Run the .agents setup script to re-sync all harness configuration.

```bash
~/.agents/setup
```

Report what was linked, updated, or pruned.
