# context/

Markdown files containing standing directives that apply across all projects. Symlinked into each harness's rules/steering folder by `setup`.

Add your own context files here (e.g., `deployment.md`, `architecture.md`).
