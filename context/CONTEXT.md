# Context

This directory contains standing context that is loaded into every agent session. All `.md` files here are symlinked into each harness's context folder by the `setup` script.

Add context as either:
- Additional `.md` files in this directory (each is loaded independently)
- Inline content below in this file

## Guidelines

<!-- Add your own standing directives, conventions, or project context below -->
